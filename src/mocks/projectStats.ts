import { ProjectStats } from "../models/projectStats";
export const projectStatsData: ProjectStats = {
  numberOfProjects: 2,
  minimumAmount: 20000,
  maximumAmount: 150000,
  averageAmount: 85000,
};
